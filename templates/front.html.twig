<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>Advisora | Conseil financier intelligent</title>
  <meta name="description" content="Advisora - Conseil financier alimenté par l'IA pour les entreprises modernes. Solutions d'investissement, analyses prédictives et accompagnement personnalisé.">
  <!-- Google Fonts + Font Awesome Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700;14..32,800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <!-- Chart.js for interactive charts -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
  <!-- External CSS -->
  <link rel="stylesheet" href="/css/front.css">
</head>
<body>

<!-- TUNISIAN DINAR (TND) CURRENCY TICKER -->
<div class="top-currency-bar">
  <div class="currency-ticker" id="forexTicker">
    <span><i class="fas fa-coins"></i> <span class="ticker-label">🇹🇳 TND/USD:</span> <span id="tndusd">0.3215</span> <span id="tndusdChange" class="positive">▲ +0.18%</span></span>
    <span><i class="fas fa-euro-sign"></i> <span class="ticker-label">TND/EUR:</span> <span id="tndeur">0.2950</span> <span id="tndeurChange" class="positive">▲ +0.07%</span></span>
    <span><i class="fas fa-pound-sign"></i> <span class="ticker-label">TND/GBP:</span> <span id="tndgbp">0.2532</span> <span id="tndgbpChange" class="negative">▼ -0.11%</span></span>
    <span><i class="fas fa-chart-line"></i> <span class="ticker-label">TND/JPY:</span> <span id="tndjpy">48.62</span> <span id="tndjpyChange" class="positive">▲ +0.23%</span></span>
    <span><i class="fas fa-franc-sign"></i> <span class="ticker-label">TND/CHF:</span> <span id="tndchf">0.2845</span> <span id="tndchfChange" class="negative">▼ -0.05%</span></span>
    <span><i class="fas fa-dollar-sign"></i> <span class="ticker-label">TND/CAD:</span> <span id="tndcad">0.4370</span> <span id="tndcadChange" class="positive">▲ +0.09%</span></span>
    <span><i class="fas fa-chart-simple"></i> <span class="ticker-label">TND/CNY:</span> <span id="tndcny">2.3210</span> <span id="tndcnyChange" class="negative">▼ -0.03%</span></span>
  </div>
  <div class="ticker-subtitle">
    <i class="fas fa-chart-line"></i> Dinar Tunisien (TND) · Taux de change en direct
  </div>
</div>

<header>
  <div class="logo">
    <img src="/assets/logo.png" alt="Advisora Logo" class="logo-img" onerror="this.style.display='none'">
    Advisora
  </div>
  <nav>
    <a href="#about" class="nav-link">À propos</a>
    <a href="#solutions" class="nav-link">Solutions</a>
    <a href="#whyus" class="nav-link">Pourquoi Advisora ?</a>
    <a href="#reviews" class="nav-link">Réussites</a>
    <a href="#contact" class="nav-link">Contact</a>
  </nav>
  <div>
    <button class="btn btn-outline" id="loginBtn"><i class="far fa-user"></i> Connexion</button>
    <button class="btn btn-primary" id="getStartedBtn">Commencer →</button>
  </div>
</header>

<!-- HERO SECTION -->
<section class="hero">
  <div class="hero-text">
    <span class="badge"><i class="fas fa-rocket"></i> Conseil financier nouvelle génération</span>
    <h1>Des décisions éclairées pour une croissance durable</h1>
    <p>Alliez la puissance de l'IA à l'expertise humaine pour transformer vos données en opportunités concrètes.</p>
    <button class="btn btn-primary" id="heroCta">Découvrir nos solutions <i class="fas fa-arrow-right"></i></button>
    <div class="hero-stats">
      <div class="stat"><span class="stat-number">2,4 Mds €</span><span>Actifs sous conseil</span></div>
      <div class="stat"><span class="stat-number">98%</span><span>Clients satisfaits</span></div>
      <div class="stat"><span class="stat-number">15+</span><span>Prix d'excellence</span></div>
    </div>
  </div>
  <div class="hero-image">
    <img src="https://images.unsplash.com/photo-1551288049-bebda4e38f71?w=800&auto=format" alt="Analyse financière">
    <div class="floating-card">
      <i class="fas fa-chart-line"></i> <span>Performance +32% en moyenne</span>
    </div>
  </div>
</section>

<!-- ABOUT SECTION enrichi -->
<section id="about" class="about-section">
  <div class="about-container">
    <div class="about-text">
      <h2 class="section-title" style="text-align: left;">Qui sommes-nous ?</h2>
      <p class="about-intro">Nous sommes là pour vous guider et réaliser vos objectifs financiers avec excellence et intégrité.</p>
      <p>Fondé en 2012, Advisora est un cabinet de conseil financier nouvelle génération qui allie technologie de pointe et expertise humaine. Notre mission : démocratiser l'accès à des conseils financiers de qualité supérieure, grâce à l'intelligence artificielle et à l'analyse prédictive.</p>
      <div class="about-highlight">
        <i class="fas fa-handshake"></i>
        <div>
          <strong>Notre engagement</strong>
          <p>Accompagner chaque client avec transparence, innovation et résultats mesurables.</p>
        </div>
      </div>
      <div class="about-stats">
        <div class="about-stat">
          <span class="about-number">12+</span>
          <span>années d'expertise</span>
        </div>
        <div class="about-stat">
          <span class="about-number">350+</span>
          <span>entreprises accompagnées</span>
        </div>
        <div class="about-stat">
          <span class="about-number">4.9/5</span>
          <span>note moyenne clients</span>
        </div>
      </div>
    </div>
    <div class="about-image">
      <img src="https://images.unsplash.com/photo-1522071820081-009f0129c71c?w=600&auto=format" alt="Notre équipe">
      <div class="about-badge">
        <i class="fas fa-chart-line"></i> +42% ROI moyen client
      </div>
    </div>
  </div>
</section>



<!-- SOLUTIONS SECTION -->
<section id="solutions" class="solutions-section">
  <h2 class="section-title">Solutions intelligentes pour la <span style="color:#C37D5D;">croissance</span></h2>
  <p class="section-sub">De la modélisation prédictive à la gestion des risques — nous vous aidons à réussir.</p>
  <div class="feature-grid">
    <div class="card">
      <div class="card-icon"><i class="fas fa-chart-pie"></i></div>
      <h3>Analyses intelligentes</h3>
      <p>Tableaux de bord interactifs avec suivi des KPI en temps réel et signaux de marché.</p>
      <span class="card-link">En savoir plus →</span>
    </div>
    <div class="card">
      <div class="card-icon"><i class="fas fa-brain"></i></div>
      <h3>Moteur de stratégie IA</h3>
      <p>Stratégies d'investissement sur mesure utilisant l'apprentissage automatique.</p>
      <span class="card-link">En savoir plus →</span>
    </div>
    <div class="card">
      <div class="card-icon"><i class="fas fa-shield-alt"></i></div>
      <h3>Risque & conformité</h3>
      <p>Évaluation proactive des risques + outils de conformité réglementaire automatisés.</p>
      <span class="card-link">En savoir plus →</span>
    </div>
    <div class="card">
      <div class="card-icon"><i class="fas fa-charging-station"></i></div>
      <h3>Accélération de patrimoine</h3>
      <p>Optimisation de portefeuille pour haute valeur nette avec modèles fiscaux efficaces.</p>
      <span class="card-link">En savoir plus →</span>
    </div>
  </div>
</section>
<!-- WHY CHOOSE US SECTION (Pourquoi nous choisir ?) -->
<section id="whyus" class="whyus-section">
  <h2 class="section-title">Pourquoi <span style="color:#C37D5D;">nous choisir ?</span></h2>
  <p class="section-sub">Découvrez l'impact mesurable de notre accompagnement sur votre performance financière.</p>
  
  <div class="whyus-grid">
    <div class="whyus-card">
      <i class="fas fa-chart-line"></i>
      <h3>Performance prouvée</h3>
      <p>Nos clients constatent en moyenne une amélioration de <strong>+42%</strong> de leur rentabilité dans les 12 premiers mois.</p>
    </div>
    <div class="whyus-card">
      <i class="fas fa-clock"></i>
      <h3>Gain de temps précieux</h3>
      <p>Automatisation des analyses qui réduit de <strong>70%</strong> le temps consacré aux rapports financiers.</p>
    </div>
    <div class="whyus-card">
      <i class="fas fa-shield-alt"></i>
      <h3>Sécurité maximale</h3>
      <p>Protection des données de niveau bancaire et conformité RGPD certifiée.</p>
    </div>
  </div>

  <!-- Interactive Charts -->
  <div class="chart-container">
    <div class="chart-card">
      <h3><i class="fas fa-chart-line"></i> Croissance projetée du ROI</h3>
      <canvas id="roiChart" width="400" height="250"></canvas>
      <p class="chart-note">Avec Advisora vs stratégie traditionnelle - Projection sur 5 ans</p>
    </div>
    <div class="chart-card">
      <h3><i class="fas fa-chart-bar"></i> Réduction des risques</h3>
      <canvas id="riskChart" width="400" height="250"></canvas>
      <p class="chart-note">Diminution de la volatilité du portefeuille après accompagnement</p>
    </div>
  </div>

  <div class="prediction-banner">
    <div class="prediction-item">
      <span class="prediction-label">Retour sur investissement moyen</span>
      <span class="prediction-value">+38%</span>
    </div>
    <div class="prediction-item">
      <span class="prediction-label">Taux de réussite stratégique</span>
      <span class="prediction-value">92%</span>
    </div>
    <div class="prediction-item">
      <span class="prediction-label">Clients qui recommandent</span>
      <span class="prediction-value">96%</span>
    </div>
  </div>
</section>

<!-- REVIEWS SECTION -->
<section id="reviews" class="testimonials">
  <h2 class="section-title">Ce que nos <span style="color:#C37D5D;">clients disent</span></h2>
  <p class="section-sub">Des retours authentiques de dirigeants qui nous ont fait confiance.</p>
  <div class="testimonial-grid">
    <div class="testi-card">
      <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div>
      <p>“Advisora a transformé notre allocation d'actifs. Les alertes en temps réel et les insights IA nous ont donné un avantage concurrentiel indéniable.”</p>
      <div class="client">
        <img src="https://randomuser.me/api/portraits/women/68.jpg" alt="client">
        <div><strong>Elena Marchetti</strong><br>CFO, FinCore</div>
      </div>
    </div>
    <div class="testi-card">
      <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i></div>
      <p>“Des outils stratégiques exceptionnels. Notre ROI a augmenté de 27% en 6 mois grâce à leur tableau de bord et à leurs recommandations personnalisées.”</p>
      <div class="client">
        <img src="https://randomuser.me/api/portraits/men/32.jpg" alt="client">
        <div><strong>James O'Connor</strong><br>Directeur stratégie</div>
      </div>
    </div>
    <div class="testi-card">
      <div class="stars"><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star"></i><i class="fas fa-star-half-alt"></i></div>
      <p>“Moderne, élégant et incroyablement perspicace. L'analytique prédictive nous a évité des pertes majeures et optimisé nos investissements.”</p>
      <div class="client">
        <img src="https://randomuser.me/api/portraits/women/45.jpg" alt="client">
        <div><strong>Priya Kapoor</strong><br>Directrice d'investissement</div>
      </div>
    </div>
  </div>
</section>

<!-- CTA BANNER -->
<div class="cta-banner">
  <div class="cta-text">
    <h2>Prêt à transformer votre approche financière ?</h2>
    <p>Rejoignez les entreprises qui font confiance à Advisora pour une croissance durable et performante.</p>
  </div>
  <button class="btn btn-primary" id="ctaBannerBtn">Planifier une démo →</button>
</div>

<!-- FOOTER -->
<footer id="contact">
  <div class="footer-grid">
    <div class="footer-col">
      <h3><i class="fas fa-chart-line" style="color:#C37D5D"></i> Advisora</h3>
      <p>Écosystème financier intelligent<br>© 2026 Tous droits réservés</p>
      <div class="social-icons">
        <a href="#" aria-label="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
        <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
        <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
      </div>
    </div>
    <div class="footer-col">
      <h4>Plateforme</h4>
      <p>Analytique<br>Stratégies<br>Accès API<br>Tarifs</p>
    </div>
    <div class="footer-col">
      <h4>Ressources</h4>
      <p>Blog<br>Études de cas<br>Webinaires<br>Documentation</p>
    </div>
    <div class="footer-col newsletter">
      <h4>Lettre d'information</h4>
      <input type="email" id="newsEmail" placeholder="Votre email professionnel">
      <button class="btn btn-primary" id="newsSignupBtn">S'abonner <i class="fas fa-paper-plane"></i></button>
      <p>Recevez nos insights hebdomadaires, sans spam.</p>
    </div>
  </div>
</footer>

<script>
  // Smooth scroll function with offset for fixed header
  function smoothScroll(targetElement, duration = 800) {
    const targetPosition = targetElement.getBoundingClientRect().top;
    const startPosition = window.pageYOffset;
    const distance = targetPosition - 80;
    let startTime = null;

    function animation(currentTime) {
      if (startTime === null) startTime = currentTime;
      const timeElapsed = currentTime - startTime;
      const run = easeInOutCubic(timeElapsed, startPosition, distance, duration);
      window.scrollTo(0, run);
      if (timeElapsed < duration) requestAnimationFrame(animation);
    }

    function easeInOutCubic(t, b, c, d) {
      t /= d / 2;
      if (t < 1) return c / 2 * t * t * t + b;
      t -= 2;
      return c / 2 * (t * t * t + 2) + b;
    }

    requestAnimationFrame(animation);
  }

  function scrollToSection(sectionId) {
    const section = document.querySelector(sectionId);
    if (section) {
      smoothScroll(section, 600);
    }
  }

  // Navigation links
  document.querySelectorAll('.nav-link').forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const targetId = link.getAttribute('href');
      scrollToSection(targetId);
    });
  });

  // Hero CTA button
  const heroCtaBtn = document.getElementById('heroCta');
  if (heroCtaBtn) {
    heroCtaBtn.addEventListener('click', () => {
      scrollToSection('#solutions');
    });
  }

  // Get Started button
  const getStartedBtn = document.getElementById('getStartedBtn');
  if (getStartedBtn) {
    getStartedBtn.addEventListener('click', () => {
      scrollToSection('#solutions');
    });
  }

  // Toast notification
  function showToastMessage(msg) {
    alert(msg + ' ✨');
  }

  // Primary buttons handlers
  const btnsPrimary = document.querySelectorAll('.btn-primary');
  btnsPrimary.forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      if(btn.id === 'newsSignupBtn') {
        const emailInput = document.getElementById('newsEmail');
        if(emailInput.value && emailInput.value.includes('@')) {
          showToastMessage(`📬 Merci ! ${emailInput.value} est maintenant abonné.`);
          emailInput.value = '';
        } else {
          showToastMessage('📧 Veuillez entrer une adresse email valide.');
        }
      } else if(btn.id === 'ctaBannerBtn') {
        showToastMessage('🎯 Demande de démo reçue ! Notre équipe vous contactera sous 24h.');
      } else if(btn.id === 'heroCta' || btn.id === 'getStartedBtn') {
        return;
      } else {
        showToastMessage('Fonctionnalité à venir — version améliorée en route.');
      }
    });
  });

  // Login button
  const loginBtn = document.getElementById('loginBtn');
  if(loginBtn) {
    loginBtn.addEventListener('click', () => {
      showToastMessage('🔐 Accès sécurisé — connectez-vous avec SSO ou email.');
    });
  }

  // Card links
  const cardLinks = document.querySelectorAll('.card-link');
  cardLinks.forEach(link => {
    link.addEventListener('click', () => {
      showToastMessage('📈 Plongez plus profond : livres blancs et études de cas disponibles.');
    });
  });

  // Initialize Charts
  window.addEventListener('load', () => {
    // ROI Chart
    const roiCtx = document.getElementById('roiChart').getContext('2d');
    new Chart(roiCtx, {
      type: 'line',
      data: {
        labels: ['Année 1', 'Année 2', 'Année 3', 'Année 4', 'Année 5'],
        datasets: [
          {
            label: 'Avec Advisora',
            data: [12, 28, 45, 68, 92],
            borderColor: '#C37D5D',
            backgroundColor: 'rgba(195, 125, 93, 0.1)',
            borderWidth: 3,
            fill: true,
            tension: 0.3,
            pointBackgroundColor: '#C37D5D',
            pointRadius: 5,
            pointHoverRadius: 7
          },
          {
            label: 'Stratégie traditionnelle',
            data: [5, 11, 18, 26, 35],
            borderColor: '#7D8F6E',
            backgroundColor: 'rgba(125, 143, 110, 0.05)',
            borderWidth: 2,
            fill: true,
            tension: 0.3,
            pointBackgroundColor: '#7D8F6E',
            pointRadius: 4,
            pointHoverRadius: 6
          }
        ]
      },
      options: {
        responsive: true,
        maintainAspectRatio: true,
        plugins: {
          legend: { position: 'top', labels: { font: { size: 12, family: 'Inter' } } },
          tooltip: { backgroundColor: '#2C2418', titleColor: '#E8DDD0', bodyColor: '#CBBEAE' }
        },
        scales: {
          y: { title: { display: true, text: 'ROI (%)', font: { size: 12 } }, beginAtZero: true, grid: { color: '#F0E7DC' } },
          x: { grid: { display: false } }
        }
      }
    });

    // Risk Reduction Chart
    const riskCtx = document.getElementById('riskChart').getContext('2d');
    new Chart(riskCtx, {
      type: 'bar',
      data: {
        labels: ['Avant Advisora', 'Après 6 mois', 'Après 12 mois', 'Après 24 mois'],
        datasets: [{
          label: 'Indice de volatilité',
          data: [78, 52, 34, 21],
          backgroundColor: '#C37D5D',
          borderRadius: 10,
          barPercentage: 0.6,
          hoverBackgroundColor: '#A56143'
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: true,
        plugins: {
          legend: { position: 'top' },
          tooltip: { backgroundColor: '#2C2418' }
        },
        scales: {
          y: { title: { display: true, text: 'Niveau de risque (%)', font: { size: 12 } }, beginAtZero: true, max: 100, grid: { color: '#F0E7DC' } },
          x: { grid: { display: false } }
        }
      }
    });
  });

  // TND Currency ticker simulation
  function updateTNDTicker() {
    let tndusd = parseFloat(document.getElementById('tndusd').innerText);
    let tndeur = parseFloat(document.getElementById('tndeur').innerText);
    let tndgbp = parseFloat(document.getElementById('tndgbp').innerText);
    let tndjpy = parseFloat(document.getElementById('tndjpy').innerText);
    let tndchf = parseFloat(document.getElementById('tndchf').innerText);
    let tndcad = parseFloat(document.getElementById('tndcad').innerText);
    let tndcny = parseFloat(document.getElementById('tndcny').innerText);

    const randomMove = () => (Math.random() - 0.5) * 0.0015;
    const randomMoveJpy = () => (Math.random() - 0.5) * 0.25;
    const randomMoveCny = () => (Math.random() - 0.5) * 0.008;

    let newTNDUSD = +(tndusd + randomMove()).toFixed(4);
    let newTNDEUR = +(tndeur + randomMove()).toFixed(4);
    let newTNDGBP = +(tndgbp + randomMove()).toFixed(4);
    let newTNDJPY = +(tndjpy + randomMoveJpy()).toFixed(2);
    let newTNDCHF = +(tndchf + randomMove()).toFixed(4);
    let newTNDCAD = +(tndcad + randomMove()).toFixed(4);
    let newTNDCNY = +(tndcny + randomMoveCny()).toFixed(4);

    let changeUSD = ((newTNDUSD - tndusd) / tndusd * 100).toFixed(2);
    let changeEUR = ((newTNDEUR - tndeur) / tndeur * 100).toFixed(2);
    let changeGBP = ((newTNDGBP - tndgbp) / tndgbp * 100).toFixed(2);
    let changeJPY = ((newTNDJPY - tndjpy) / tndjpy * 100).toFixed(2);
    let changeCHF = ((newTNDCHF - tndchf) / tndchf * 100).toFixed(2);
    let changeCAD = ((newTNDCAD - tndcad) / tndcad * 100).toFixed(2);
    let changeCNY = ((newTNDCNY - tndcny) / tndcny * 100).toFixed(2);

    function setChange(elementId, changeVal) {
      let span = document.getElementById(elementId);
      if(span) {
        let num = parseFloat(changeVal);
        let isPositive = num >= 0;
        span.innerText = (isPositive ? `▲ +${Math.abs(num)}%` : `▼ -${Math.abs(num)}%`);
        span.className = isPositive ? 'positive' : 'negative';
      }
    }

    document.getElementById('tndusd').innerText = newTNDUSD;
    document.getElementById('tndeur').innerText = newTNDEUR;
    document.getElementById('tndgbp').innerText = newTNDGBP;
    document.getElementById('tndjpy').innerText = newTNDJPY;
    document.getElementById('tndchf').innerText = newTNDCHF;
    document.getElementById('tndcad').innerText = newTNDCAD;
    document.getElementById('tndcny').innerText = newTNDCNY;

    setChange('tndusdChange', changeUSD);
    setChange('tndeurChange', changeEUR);
    setChange('tndgbpChange', changeGBP);
    setChange('tndjpyChange', changeJPY);
    setChange('tndchfChange', changeCHF);
    setChange('tndcadChange', changeCAD);
    setChange('tndcnyChange', changeCNY);
  }

  function randomizeTNDInitial() {
    let ids = ['tndusd', 'tndeur', 'tndgbp', 'tndjpy', 'tndchf', 'tndcad', 'tndcny'];
    ids.forEach(id => {
      let el = document.getElementById(id);
      if(el) {
        let base = parseFloat(el.innerText);
        let variance = base * (Math.random() - 0.5) * 0.003;
        let newVal = (base + variance).toFixed(id === 'tndjpy' ? 2 : 4);
        el.innerText = newVal;
      }
    });
    updateTNDTicker();
  }

  randomizeTNDInitial();
  let tickerInterval = setInterval(updateTNDTicker, 4500);
  window.addEventListener('beforeunload', () => {
    if(tickerInterval) clearInterval(tickerInterval);
  });
</script>
</body>
</html>