{% extends 'base_back.html.twig' %}

{% block title %}Advisora | {{ page_title }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/project-management.css') }}">
     <link rel="stylesheet" href="{{ asset('css/admin-custom.css') }}?v=20260415-users-v2">
    <link rel="stylesheet" href="{{ asset('css/resource-backoffice.css') }}?v=20260417-rs-v29">
{% endblock %}

{% macro field(field) %}
    <div class="pm-field rs-form-field {% if field.vars.errors|length > 0 %}pm-field-has-error{% endif %}">
        {{ form_label(field) }}
        {{ form_widget(field) }}
        {% if field.vars.help %}
            <div class="pm-field-help">{{ form_help(field) }}</div>
        {% endif %}
        <div class="pm-field-errors" data-field-errors>
            {{ form_errors(field) }}
        </div>
    </div>
{% endmacro %}

{% block body %}
    {% import _self as forms %}
    <section class="pm-admin-shell rs-admin-shell rs-module-resource">
        <div class="pm-admin-header rs-header">
            <div class="rs-header-copy back-users-title">
                <span class="pm-badge">Back office</span>
                <h1>{{ page_title }}</h1>
                <p>{{ page_message|default('Renseignez les informations de la ressource.') }}</p>
            </div>
            <div class="pm-card-actions rs-header-actions">
                <a class="pm-btn pm-btn-secondary" href="{{ resource.id ? path(back_route|default('resource_back_manage'), {id: resource.id}) : path('back_resource_index') }}">Retour</a>
            </div>
        </div>

        <section class="pm-form-card rs-panel rs-form-panel">
            <div class="pm-section-head rs-panel-head">
                <div>
                    <h2>Saisie</h2>
                    <p>Champs obligatoires du catalogue ressource.</p>
                </div>
                <div class="pm-card-actions">
                    <a class="pm-btn pm-btn-secondary" href="{{ path('supplier_new') }}">Ajouter un fournisseur</a>
                </div>
            </div>
            <div class="rs-form-meta">
                <span><strong>Statut actuel :</strong> {{ resource.statusLabel ?: 'Sera determine a l enregistrement.' }}</span>
                <span><strong>Reservations liees :</strong> {{ resource.id ? resource.projects|length : 0 }}</span>
            </div>
            {{ form_start(form, {attr: {novalidate: 'novalidate', class: 'pm-validated-form rs-form', 'data-validation-form': 'resource'}}) }}
                {{ form_errors(form) }}
                <div class="rs-form-grid">
                    {{ forms.field(form.nomRs) }}
                    {{ forms.field(form.cataloguefournisseur) }}
                    {{ forms.field(form.prixRs) }}
                    {{ forms.field(form.QuantiteRs) }}
                    {{ forms.field(form.availabilityStatusRs) }}
                    {{ forms.field(form.imageUrlRs) }}
                </div>
                <div class="pm-form-actions rs-form-actions">
                    {{ form_widget(form.save, {attr: {class: 'pm-btn pm-btn-primary'}}) }}
                    <a class="pm-btn pm-btn-secondary" href="{{ resource.id ? path(back_route|default('resource_back_manage'), {id: resource.id}) : path('back_resource_index') }}">Annuler</a>
                </div>
            {{ form_end(form) }}
        </section>
    </section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/project-form-validation.js') }}"></script>
{% endblock %}
